#!/sbin/openrc-run
depend() {
    need localmount
    after modules sysfs
    before sshd
}
start() {
    ebegin "Fixing /var/empty permissions"
    mkdir -p /var/empty
    chown root:root /var/empty
    chmod 555 /var/empty
    eend $?
}